{
  "name": "AngularVRT",
  "version": "0.0.0",
  "description": "Visual regression testing angular apps",
  "repository": "https://github.com/michalonderko/AngularVRT.git",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.12",
    "http-server": "^0.7.4",
    "protractor": "^1.6.1"
  },
  "scripts": {

    "start": "http-server demo-app -a localhost -p 8234 -s -c-1",

    "test": "http-server visual-regression-test -a localhost -p 8999 -o -s -c-1"
  }
}
